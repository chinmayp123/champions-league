# local secrets — never commit your odds API key
odds.config.json
.env

# personal betting log — keep your picks/results out of the (public) repo
bets/

node_modules/

# firebase: the publisher account's local login (publisher/firestore.mjs), CLI caches/logs
publisher/credentials.json
.firebase/
# the built website (web/build.mjs) and the Vercel CLI's project link
site/
.vercel/
firebase-debug*.log
firestore-debug.log

# scratch / build
mockup.html
.playwright-mcp/
dist/

# local morning-routine launchers (machine-specific paths)
run-morning.bat
run-morning.vbs

.vercel
.env*
